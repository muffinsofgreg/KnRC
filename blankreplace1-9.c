#include <stdio.h>

/* copy input to output, replace each string of one or more blanks
 * by a single blank */

int main(void) {
    int c;
    int nl = 0;

    while ((c = getchar()) != EOF) {
        if (c == '\n')
            ++nl;
            
    }
}
